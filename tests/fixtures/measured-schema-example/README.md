# Test-Only Run Table

`runs.csv` contains deterministic fictional observations used only to test the
processed measured-data schema and paired-analysis logic. It is not empirical
research evidence and must never be copied into `data/` or published results.
